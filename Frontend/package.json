{
  "name": "todolist",
  "version": "1.0.0",
  "description": "Todo List using JS+HTML+CSS for frontend and golang for backend and MySql for the database",
  "default": "index.html",
  "scripts": {
    "dev": "parcel src/*.html",
    "build": "parcel build src/*.html --dist-dir ./dist"
  },
  "author": "Andreas Meier",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.12.0"
  },
  "devDependencies": {
    "@parcel/packager-raw-url": "^2.12.0",
    "@parcel/transformer-webmanifest": "^2.12.0"
  }
}
